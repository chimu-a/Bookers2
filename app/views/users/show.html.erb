<div>
  <%= flash[:notice] %>
</div>

<p id="notice"></p>
<div class="container px-5 px-sm-0">
  <div class="row">

    <%= render 'books/book', book:  @book_new, user: @user %>
    <div class="col-md-8 offset-md-1">

  <h2>Books</h2>
  <table class="table tavble-hover table-inverse">
    <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th coldpan="3"></th>
      </tr>
    </thead>

    <% @books.each do |book| %>

    <tbody>
      <tr>
　　    <td><%= image_tag @user.get_profile_image(100,100) %></td>
　　    <td><%= link_to book.title, book_path(book.id) %></td>
        <td><%= book.body %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
</div>
</div>







